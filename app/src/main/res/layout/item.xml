<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="60dp" android:orientation="horizontal">

    <TextView
            android:id="@+id/item_title"
            android:layout_height="wrap_content"
            android:text="Task title"
        android:layout_alignTop="@id/submit"
        android:layout_below="@id/submit"
            android:textSize="18sp"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginStart="12dp"  android:layout_width="260dp"/>

    <ImageView
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:layout_marginRight="50dp"
        android:id="@+id/reverse"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:src="@drawable/ic_settings_backup_restore_black_24dp"></ImageView>
    <ImageView
        android:layout_width="30dp"
        android:layout_height="wrap_content"
        android:id="@+id/submit"
        android:layout_alignParentRight="true"
        android:layout_toLeftOf="@id/reverse"
        android:layout_centerVertical="true"
        android:layout_marginRight="20dp"
        android:src="@drawable/ic_done_black_24dp"></ImageView>


</RelativeLayout>